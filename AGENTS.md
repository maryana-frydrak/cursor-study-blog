# StudySprint

`client/` — React + Vite (5173). `server/` — Express + MongoDB Docker (5001).

```bash
cd server && npm run setup && npm run server
cd client && npm run dev   # VITE_BASE_URL=http://localhost:5001
```

Conventions: `.cursor/rules/*.mdc`. Admin login: root `README.md`.
